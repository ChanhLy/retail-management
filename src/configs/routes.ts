import { Router } from 'express';
import glob from 'glob';

const router = Router();
// router.route('/api');
// router.use(require('../routes/index.routes.ts').default);
// glob('**/*', { realpath: true }, (err, files: string[]) => {
//   files.forEach((file: string) => {
//     if (file.includes('.routes.ts')) {
//       console.log(file);
//       router.use(require(file).default);
//     }
//   });
// });
// router.use(
//   require('/mnt/work/svn/retail-management/src/users/users.routes.ts').default,
// );

export default router;
